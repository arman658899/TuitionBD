<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brogrammers.tutionbd">

    <application
        android:name=".AppInitializer"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.UserTypeActivity" />
        <activity android:name=".views.loginactivity.LoginActivity" />
        <activity android:name=".views.otpactivity.OtpVarificationActivity"/>
        <activity android:name=".views.mainactivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.introslideractivity.IntroSliderActivity" />
        <activity android:name=".views.SplashScreen" />
    </application>

</manifest>